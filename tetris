#include "block.h"
#include <Windows.h>
#include <stdio.h>

int main() {
	HANDLE hConsoleOut = GetStdHandle(STD_OUTPUT_HANDLE);
	//바닥
	for (int i = 0; i < 11; i++) {
		COORD pos1 = { i, 12 };
		SetConsoleCursorPosition(hConsoleOut, pos1);
		printf("■");
	}


}
